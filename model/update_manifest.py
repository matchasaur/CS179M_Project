def update(manifest, operations):
    for line in manifest:
        

if __name__ == "__main__":
    manifest = open('ShipCase3.txt', 'w')
    operations = open('operations.txt', 'r')

    update(manifest, operations)
